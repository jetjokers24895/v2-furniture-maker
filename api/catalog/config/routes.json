{
  "routes": [
    {
      "method": "GET",
      "path": "/catalog",
      "handler": "Catalog.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/catalog/count",
      "handler": "Catalog.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/catalog/:_id",
      "handler": "Catalog.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/catalog",
      "handler": "Catalog.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/catalog/:_id",
      "handler": "Catalog.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/catalog/:_id",
      "handler": "Catalog.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
