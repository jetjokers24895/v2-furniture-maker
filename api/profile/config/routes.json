{
  "routes": [
    {
      "method": "PUT",
      "path": "/profile/update-user/:id",
      "handler": "Profile.updateUser",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/profile/change-password/:id",
      "handler": "Profile. ",
      "config": {
        "policies": []
      }
    }
  ]
}